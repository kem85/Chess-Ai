"""
Setup configuration shim for chess-ai-arena package.
Allows compatibility with older pip/setuptools versions while pyproject.toml defines configuration.
"""

from setuptools import setup

setup()
